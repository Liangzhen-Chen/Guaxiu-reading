name: Deploy to ECS

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      target:
        description: '部署目标'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ECS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H 122.51.236.219 >> ~/.ssh/known_hosts

      - name: Deploy
        run: |
          bash deploy.sh ${{ github.event.inputs.target || 'production' }}

      - name: Health Check
        run: |
          sleep 5
          curl -s https://xiugua-reading.cn/health | grep '"ok"'
